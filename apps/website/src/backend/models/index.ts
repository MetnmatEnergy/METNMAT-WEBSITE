/**
 * Backend domain models.
 * Re-exported from the shared @metnmat/types package so the website, backend and
 * future codebases all agree on the same shapes (no type drift).
 *
 * Add backend-only types (that should NOT be shared) directly in this file.
 */
export type { Enquiry, Product, BlogPost, ApiResult } from "@metnmat/types";
