# Static, NON-secret environment for metnmat-cc.service (MTrace Command Center dashboard).
# Everything that used to be in the plaintext .env now lives in metnmat/cc/env.
NODE_ENV=production
PORT=3000
NODE_OPTIONS=--max-old-space-size=1536
NEXT_TELEMETRY_DISABLED=1
NEXTAUTH_URL=https://command-center.metnmat.com
