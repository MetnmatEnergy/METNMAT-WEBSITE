# Mirrors assertProductionConfig() in apps/dashboard/src/payload.config.ts, plus the
# CMS-only S3 access key (the host role has no media access by design).
MONGODB_URI
PAYLOAD_SECRET
PAYLOAD_PIN_PEPPER
S3_ACCESS_KEY_ID
S3_SECRET_ACCESS_KEY
