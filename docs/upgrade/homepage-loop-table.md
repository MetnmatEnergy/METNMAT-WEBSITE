# Homepage main-thread loop inventory

Generated 2026-09-03 by a 30-agent adversarial sweep of every continuously-running mechanism reachable from `/`. Deduped by file+function; 124 distinct rows from 126 reports. The single `critical` row was the Chrome "Page Unresponsive" incident and is fixed in `0e5dc5a`.

| Risk | File | Function | Loop type | Frequency | Work per tick | Visibility gated | Cleanup |
|---|---|---|---|---|---|---|---|
| critical | `frontend/components/chat/chat-widget.tsx` | ChatWidget a11y effect — MutationObserver(apply) on document.body (lines 122-163) | MutationObserver microtask self-trigger (setAttribute inside callback on observed subtree) | Unbounded: one microtask per iteration, never returns to the event loop once #chat-widget-container has a <button> and #chat-widget-frame-co | 2x getElementById + 2x querySelector + getComputedStyle(panel).display (forced style recalc) + setAttribute('aria-expanded') which queues th | No (arms after first pointer/key/touch post-consent, or the 20 s backstop while visible) | observer.disconnect() on unmount/wake change — unreachable while the microtask queue never drains |
| critical | `frontend/components/chat/chat-widget.tsx (HEAD 03c1d5f)` | useEffect [wake] -> MutationObserver(apply) on document.body | MutationObserver microtask (self-feeding via unconditional setAttribute) | Unbounded: re-queues itself every microtask once the widget DOM exists; also fired by every attribute/childList mutation anywhere in body (f | getElementById + 2x querySelector + getComputedStyle(panel).display (forced style recalc, aria-expanded write dirties style via [aria-expand | No (arms on first interaction or 20 s backstop after consent; never gated afterwards) | observer.disconnect() on unmount only; the layout never unmounts it |
| critical | `frontend/components/chat/chat-widget.tsx` | MutationObserver(document.body, {childList, subtree, attributes}) → apply() (HEAD 122-163) | MutationObserver (microtask), self-feeding via unconditional setAttribute | Every DOM mutation anywhere under <body> before the widget mounts; UNBOUNDED (thousands/s, never yields) once #chat-widget-container has a < | 2x getElementById, 2x querySelector, getComputedStyle(panel).display (style recalc), launcher.setAttribute('aria-expanded') — the write re-q | No (arms on first pointer/keyboard/touch event after a consent decision; independent of tab visibility) | disconnect on effect cleanup, but the component lives in the root layout and `wake` never resets, so it runs for the page lifetime |
| critical | `frontend/components/chat/chat-widget.tsx` | useEffect [wake] — MutationObserver(apply) on document.body {childList, subtree, attributes} | MutationObserver (microtask), self-feeding | Every DOM mutation batch site-wide; once the widget DOM exists, continuously (each callback queues the next) — ~30k-100k iterations/s, never | getElementById ×2, querySelector ×2, getComputedStyle().display, setAttribute('aria-expanded') unconditionally (+ 2 guarded setAttribute) | No | observer.disconnect() on unmount — unreachable once the loop starts because unmount is a task the microtask chain never yields to |
| critical | `frontend/components/chat/chat-widget.tsx (HEAD 92f7c57 = production)` | ChatWidget a11y effect: new MutationObserver(apply).observe(document.body, {childList, subtree, attributes}) | MutationObserver microtask feedback loop (callback writes aria-expanded inside the observed subtree) | Unbounded: one microtask per iteration, ~3k-10k/s, never yields to the event loop | 2x getElementById, 2x querySelector, getComputedStyle (forced style recalc), setAttribute | No (arms after consent + first pointermove/keydown/touch or 20 s; independent of route, scroll or tab visibility) | observer.disconnect() only on unmount — never, since ChatWidget is in the root layout |
| critical | `frontend/components/chat/chat-widget.tsx` | MutationObserver callback -> apply() (HEAD lines 122-163) | MutationObserver on document.body {childList, subtree, attributes:true}; self-retriggering microtask loop | Unbounded: every iteration queues the next microtask; ~30k-100k iterations/s with no yield to rendering/input/tasks | getElementById x2, querySelector x2, getComputedStyle(panel) forcing a style recalc after the attribute write, launcher.setAttribute("aria-e | No (only gated by consent decided + first interaction/20 s backstop + widget.js having built its DOM) | observer.disconnect() on unmount (line 162) - never reached, the loop never yields |
| critical | `frontend/components/chat/chat-widget.tsx (revision 03c1d5f..24b52f1)` | a11y MutationObserver in the `wake` effect (apply → launcher.setAttribute) | MutationObserver on document.body {childList, subtree, attributes} re-triggered by its own setAttribute — microtask loop | unbounded; every iteration queues the next inside the same microtask checkpoint | 2 getElementById + 2 querySelector + getComputedStyle (style recalc) + setAttribute | no — starts after the first pointer/keyboard/touch event once consent is decided; runs in background tabs too | observer.disconnect() on unmount only — never reached because the loop never yields |
| high | `frontend/components/ui/vapour-text-effect.tsx` | animate() rAF loop, number canvas x3 (VaporSlot NUM_STAGE, font 36px bold, 169x44 CSS) | requestAnimationFrame self-rescheduling (:174-269) | 60 fps per canvas while in view; state machine vaporizing ~2 s / fadingIn 1 s / waiting 3.4 s per ~6.4 s cycle | clearRect + renderParticles over all particles; idle (DPR2, measured): ~7.2-7.8k particles -> ~1.3-1.4k fillStyle + ~1.4-1.5k fillRect; vapo | yes - IntersectionObserver threshold 0 rootMargin 50px (:774-778) stops the loop out of view; document.hidden short-circuits the body (:183- | effect cleanup cancelAnimationFrame(frameId) on animationState/isInView change and unmount (:271-275) |
| high | `frontend/components/ui/vapour-text-effect.tsx` | animate() rAF loop, label canvas x3 (VaporSlot LABEL_STAGE, font 12px regular, 169x36 CSS) | requestAnimationFrame self-rescheduling (:174-269) | 60 fps per canvas while in view; same ~6.4 s state cycle | clearRect + renderParticles; idle (DPR2, measured): 3.4-5.5k particles, of which 55-60% are antialiased edges -> ~2.4-3.7k fillStyle + ~2.5- | yes - same IO + document.hidden gates | same effect cleanup (:271-275) |
| medium | `frontend/components/ui/vapour-text-effect.tsx` | VaporizeTextCycle animate() rAF loop (lines 168-287) — 6 instances on '/' | requestAnimationFrame (self-rescheduling) | Display refresh (60 Hz) per canvas while isInView; recreated on every animationState change (cleanup runs first) | static/waiting: renderParticles over all particles (~31k across six at DPR 2 -> ~20k fillRect + ~14k fillStyle); vaporizing: updateParticles | Yes: document.hidden early-return (:183) and IntersectionObserver isInView (rootMargin 50px) stops the loop off-screen | cancelAnimationFrame(frameId) in effect cleanup (:271-275) on deps change and unmount |
| medium | `frontend/components/ui/vapour-text-effect.tsx` | ResizeObserver on wrapper (x6) | ResizeObserver callback (:319-341) - direct renderCanvas + identity-guarded setWrapperSize | once at observe(); then only on genuine wrapper size change (viewport resize / scrollbar toggle). Verified: cannot self-trigger, wrapper box | full getImageData (66,924 px at DPR2) + scan + rebuild all particles, twice per canvas on a real change (direct call + effect :289) | no | resizeObserver.disconnect() (:342-344) |
| medium | `frontend/components/ui/vapour-text-effect.tsx` | animate() rAF loop (lines 168-287), x6 instances via hero-stats.tsx VaporSlot | requestAnimationFrame | 60 fps per canvas while in view (6 canvases); effect re-arms on each animationState change | clearRect + renderParticles over every particle (~7.7k number + ~3.9k label particles per slot at DPR 2/globalDpr 3 since sampleRate=1; ~35k | Yes: document.hidden short-circuit (line 183) and IntersectionObserver rootMargin 50px on the viewport (not header-aware) | cancelAnimationFrame in effect cleanup (271-275) and on out-of-view (160-163) |
| medium | `frontend/components/ui/vapour-text-effect.tsx (×6 on desktop)` | animate() rAF loop (lines 168-287) — other vector, listed for completeness | requestAnimationFrame, restarted on every animationState change | 60 fps while isInView; early-returns on document.hidden | static/waiting: run-merged fillRects (~1-3k per canvas); vaporizing (1 s of every ~5.4 s): ~N fillStyle+fillRect per particle, N ≈ 36k acros | yes — IntersectionObserver (rootMargin 50px) + document.hidden check | cancelAnimationFrame on effect cleanup; observer.disconnect() |
| low | `frontend/components/ui/vapour-text-effect.tsx` | fadingIn -> waiting setTimeout(WAIT_DURATION = 3400 ms) (lines 252-256) | setTimeout (one-shot per cycle; drives the state machine) | Once per 5.4 s cycle per canvas; can be scheduled twice if a stale frame runs before React commits 'waiting' | setAnimationState('vaporizing') + resetParticles over all particles (~10k writes per canvas) | No (fires even off-screen/unmounted; state change is inert while !isInView) | NOT cleared — id discarded; survives unmount |
| low | `frontend/components/ui/vapour-text-effect.tsx` | ResizeObserver on wrapper (lines 315-346) | ResizeObserver (observer-driven; the previously fixed feedback path) | Initial notification + every real wrapper resize (window resize frames, breakpoint changes) | setWrapperSize via nextSize (identity-preserving) + unconditional direct renderCanvas: canvas resize, getImageData over 528x132 device px, ~ | No | resizeObserver.disconnect() on unmount |
| low | `frontend/components/ui/animated-text-cycle.tsx` | AnimatedTextCycle setInterval(interval=2600) (lines 34-39) — 2 instances on '/' (eyebrow badge; mosaic badge at lg+) | setInterval + framer-motion AnimatePresence frameloop | Every 2.6 s; framer's rAF frameloop active ~0.7 s per tick (0.3 s exit + 0.4 s enter) | setIndex -> React render; framer animates opacity/y/filter:blur(8px) writing `style` at ~60 Hz (each write also hits the chat-widget MO) | No (interval throttled in background; rAF paused so animations stall until visible) | clearInterval on deps change/unmount |
| low | `frontend/components/ui/vapour-text-effect.tsx` | waiting -> vaporizing setTimeout (x6) | setTimeout(WAIT_DURATION=3400ms) scheduled from the rAF loop (:252-256) | once per cycle per canvas (~every 6.4 s) | setAnimationState + resetParticles over all particles (~7.8k / 5.5k) | no (fires while hidden/out of view; loop effect refuses to run while !isInView) | NOT cleared on unmount / out-of-view (no ref); harmless no-op after unmount |
| low | `frontend/components/ui/animated-text-cycle.tsx` | AnimatedTextCycle setInterval (x2 on the hero: eyebrow badge + mosaic badge) [outside vector - listed for completeness] | setInterval 2600 ms (:34-39) + framer-motion AnimatePresence blur/opacity/y transition | every 2.6 s, 0.4 s + 0.3 s framer JS animations with filter: blur(8px) | one re-render + ~40 framer frames animating opacity/transform/filter on a single span | no | clearInterval (:38) |
| low | `frontend/components/ui/vapour-text-effect.tsx` | animate() rAF loop (x6 canvases: 3 slots x number+label) | requestAnimationFrame | 60 fps while isInView; re-created on each animationState transition (~4 per 5.4 s cycle) | clearRect + renderParticles over ~7k (number) / ~3k (label) particles with batched fillStyle and merged opaque runs; during the 1 s 'vaporiz | Yes: document.hidden short-circuits to a no-op frame; loop not started when !isInView (IntersectionObserver, rootMargin 50px) and cancelled  | cancelAnimationFrame in effect cleanup on every deps change/unmount |
| low | `frontend/components/ui/vapour-text-effect.tsx` | useEffect [texts,font,color,alignment,wrapperSize,currentTextIndex,...] -> renderCanvas | effect-driven rebuild (getImageData + particle allocation) | Once per text change per canvas (~every 5.4 s), plus on genuine wrapperSize change | canvas.style/width/height writes, fillText, getImageData over ~57k-70k device px, per-pixel loop, ~3k-7k particle objects with 2 strings eac | Indirectly: currentTextIndex only advances while the rAF loop runs (in view, tab visible) | handleFontChange's 1 s timeout cleared on re-run |
| low | `frontend/components/ui/vapour-text-effect.tsx` | ResizeObserver on wrapper (x6) | ResizeObserver | On real wrapper resize only (fixed-height parent + minmax(0,1fr) column prevent self-trigger); ≤1 per frame during a window drag | nextSize identity check (bails) + an UNCONDITIONAL direct renderCanvas with mount-time stale closure (see low finding) | No | disconnect on unmount |
| low | `frontend/components/ui/animated-text-cycle.tsx (x2 on '/')` | setInterval(setIndex, 2600) | setInterval + framer-motion transition | Every 2.6 s; framer JS-drives opacity/y/filter(blur 8px) for 0.4 s + 0.3 s | one setState; ~40 frames of style attribute writes + blur repaint of one short text span | No (framer pauses rAF-driven animation in hidden tabs by browser throttling only) | clearInterval on deps change/unmount |
| low | `frontend/components/ui/vapour-text-effect.tsx` | animate() rAF loop — 6 instances (168-287) | requestAnimationFrame | 60/s per canvas while isInView and tab visible (≈360 rAF/s for the hero) | clearRect + renderParticles over ~5-15k particles per canvas (fillStyle per alpha bucket, opaque runs merged); updateParticles (3x Math.rand | Yes: IntersectionObserver (isInView) + document.hidden early-return | cancelAnimationFrame(frameId) in effect cleanup |
| low | `frontend/components/ui/vapour-text-effect.tsx` | ResizeObserver(wrapper) — 6 instances (319-346) | ResizeObserver | Only on a genuine contentRect change of the wrapper (wrapper has definite 100%/100% size of a fixed-height stage; the canvas child cannot ch | identity-preserving setWrapperSize + an unconditional direct renderCanvas (canvas resize, getImageData, particle rebuild ≈1-2 ms) — and a se | No | resizeObserver.disconnect() |
| low | `frontend/lib/analytics/collector.ts` | onScroll → requestAnimationFrame(sampleScroll) | scroll listener, rAF-throttled | ≤1 per frame, only while scrolling | scrollHeight + innerHeight + scrollY reads (one layout read), a division | rAF is implicitly paused in hidden tabs | Never removed (page lifetime); duplicated per consent re-grant |
| low | `frontend/lib/analytics/collector.ts` | getTracker(): pagehide / visibilitychange / click(capture) / focusin(capture) listeners + storage listener (watchConsent) | delegated event listeners | Per user event | closest() lookup, one push() (getItem + JSON.parse + array push) | N/A | storage listener guarded once; the other five are re-added on each consent re-grant and never removed |
| low | `frontend/components/chat/chat-widget.tsx (working tree, uncommitted) + lib/chat-widget-a11y.ts` | Phase A outer MutationObserver(body, childList+subtree) -> self-disconnect; Phase B inner MutationObserver(panel style attr + container chil | Event-driven MutationObserver (no self-feed: writes target attributes/elements that are not observed, and are skipped when values already ma | Phase A: one cheap getElementById per DOM-insertion batch until the widget mounts; Phase B: ~4 callbacks per chat open/close | getElementById / getComputedStyle(panel) / 0-3 guarded setAttribute | No, but bounded by widget events | outer.disconnect() + inner?.disconnect() in effect cleanup |
| low | `frontend/components/ui/vapour-text-effect.tsx` | setTimeout in fadingIn branch (WAIT_DURATION = 3.4 s) and handleFontChange 1 s timeout | one-shot timers per cycle / per mount | once per 5.4 s cycle per canvas; once per mount | resetParticles over the particle array + setState; particle rebuild | No | WAIT timeout: NOT cleared (id discarded); font timeout: cleared |
| low | `frontend/components/ui/animated-text-cycle.tsx` | setInterval(interval=2600) x2 (hero eyebrow badge, mosaic badge) + framer-motion filter/opacity transition | setInterval + framer-motion frameloop during 0.7 s transitions | 0.38 Hz; ~42 animated frames per cycle | setIndex; inline style writes (filter: blur) on one span | No (framer-motion pauses rAF in hidden tabs via browser throttling) | clearInterval |
| low | `frontend/lib/analytics/collector.ts` | getTracker() singleton: pagehide, visibilitychange, scroll (rAF-throttled sampleScroll), capture click, capture focusin, storage; 5 s flush  | module-level listeners + one-shot flush timer | per scroll frame / per click / 0.2 Hz flush | scrollHeight+innerHeight read (layout) once per frame; closest() on click; sendBeacon/fetch on flush | No | NONE by design — resetTracker() cannot remove them; re-registered on every consent re-grant |
| low | `frontend/components/ui/vapour-text-effect.tsx` | ResizeObserver on wrapper (lines 315-346), x6 | ResizeObserver | Mount + viewport width changes only (wrapper is bound by a fixed-height stage and a minmax(0,1fr) grid column) | nextSize() identity-preserving setState; then an unconditional renderCanvas(): canvas resize, fillText, getImageData over ~70k device px, al | No, but fires only on real size changes | resizeObserver.disconnect() (343) |
| low | `frontend/components/ui/vapour-text-effect.tsx` | setTimeout(WAIT_DURATION) in fadingIn branch (252-256), x6 | setTimeout (state machine step, 3.4 s) | Once per text cycle (~5.4 s) | setAnimationState + resetParticles over all particles | Indirectly (loop only runs in view) | Not cleared on unmount (fires once into a dead ref; harmless) |
| low | `frontend/components/home/hero-stats.tsx` | useDisplayStyle read() via ResizeObserver + MutationObserver on <html> class (lines 66-89), x2 | ResizeObserver + MutationObserver(attributeFilter: ["class"]) | Mount, font-swap resize of the hidden reference span, theme toggle | One getComputedStyle read, nextDisplayStyle() identity-preserving setState (no DOM writes) | No | ro.disconnect(); mo.disconnect() (85-88) |
| low | `frontend/components/home/hero-stats.tsx` | setInterval(setStep, 4000) (line 191) | setInterval | Every 4 s, only when cycleStatic (phones < 640px or prefers-reduced-motion) | One setState re-rendering three StaticSlot spans | No | clearInterval (192) |
| low | `frontend/components/ui/animated-text-cycle.tsx` | setInterval(setIndex, interval) (lines 35-39), x2 on "/" (eyebrow badge, mosaic badge; interval 2600 ms) | setInterval + framer-motion tween | Every 2.6 s; each change runs a 0.3 s exit + 0.4 s enter tween of opacity/y/filter:blur(8px->0) on one small inline span | framer-motion rAF style writes for ~0.7 s per cycle on a ~200x20 px span | No (framer-motion pauses rAF in hidden tabs by browser throttling only) | clearInterval (38) |
| low | `frontend/components/ui/info-card.tsx` | handleMouseMove (lines 28-37), x6 cards on "/" | mousemove event handler (only while hovering a card) | Pointer event rate (60-120 Hz) while hovering one card | One getBoundingClientRect + one --rotation custom-property write -> conic-gradient background repaint of that card (~380x380 CSS px, ~0.6 Mp | n/a (hover-driven) | React handler; none needed |
| low | `frontend/components/ui/vapour-text-effect.tsx (×6 on desktop)` | ResizeObserver on wrapper (lines 315-346) | ResizeObserver; callback calls renderCanvas unconditionally (lines 330-338) — but only fires on a real contentRect change since wrapper heig | initial notification + real resizes only | canvas.width/height reset + fillText + getImageData(≤528×132 device px) + one particle object per opaque pixel (~4-9k) | no (RO fires regardless of tab visibility, but only on size change) | resizeObserver.disconnect() |
| low | `frontend/components/ui/animated-text-cycle.tsx (×2 on the homepage)` | setInterval(interval=2600) + framer-motion AnimatePresence | setInterval → state change → 0.3 s exit + 0.4 s enter animation (inline style writes per frame, incl. filter: blur) | every 2.6 s; ~0.7 s of 60 fps style writes per cycle | one React re-render + framer frameloop style writes on one span | no (framer pauses rAF in hidden tabs; interval keeps ticking) | clearInterval |
| none | `frontend/components/ui/vapour-text-effect.tsx` | isInView effect setTimeout(0) -> 'vaporizing' (lines 151-165) | setTimeout one-shot | Once per isInView=true transition | one setState | Yes (only when in view) | clearTimeout in effect cleanup |
| none | `frontend/components/ui/vapour-text-effect.tsx` | handleFontChange setTimeout(1000) font re-render (lines 408-429) | setTimeout one-shot | Once per mount (first effect run); on '/' it is cancelled by the first wrapperSize update before it fires | cleanup() + renderCanvas (getImageData + particle rebuild) | No | clearTimeout via the returned cleanup on next deps change/unmount |
| none | `frontend/components/ui/vapour-text-effect.tsx` | useIsInView IntersectionObserver (lines 768-789) | IntersectionObserver | On intersection change only (threshold 0, rootMargin 50px) | one setState | n/a (it IS the gate) | observer.disconnect() on unmount |
| none | `frontend/components/home/hero-stats.tsx` | useDisplayStyle ResizeObserver(hidden span) + MutationObserver(html class) x2 (lines 60-92) | ResizeObserver + MutationObserver (read-only callbacks) | Initial + font-load/breakpoint resize of the hidden reference span; theme class toggles | getComputedStyle read + nextDisplayStyle identity-preserving setState | No | ro.disconnect(); mo.disconnect() on unmount |
| none | `frontend/components/home/hero-stats.tsx` | useMediaQuery change listeners x2 (lines 94-104) | matchMedia 'change' listener | On media-query change only | one setState | No | removeEventListener on unmount |
| none | `frontend/components/home/hero-stats.tsx` | HeroStats static cycler setInterval(4000) (lines 189-193) | setInterval | Every 4 s, ONLY when cycleStatic (prefers-reduced-motion or <640px) — mutually exclusive with the canvases | setStep((k)=>(k+1)%n) -> re-render of 3 StaticSlots | No (browser throttles to <=1/s in background) | clearInterval on deps change/unmount |
| none | `frontend/components/home/featured-projects-carousel.tsx` | FeaturedProjectsCarousel per-slide setTimeout(AUTOPLAY_MS=5500) (lines 81-85) | setTimeout keyed on `active` (effectively an interval, restarted per slide) | Every 5.5 s while autoplaying | setActive((i+1)%count) -> re-render of 15 slides (opacity class swap) + progress-bar remount (key={active}) | Yes: IntersectionObserver threshold 0.35, visibilitychange, hover/focus pause, prefers-reduced-motion | clearTimeout on deps change/unmount |
| none | `frontend/components/home/featured-projects-carousel.tsx` | IntersectionObserver (:57-65), visibilitychange (:68-73), matchMedia reduce (:47-53) | observer / event listeners | On change only | one setState each | n/a (they are the gates) | io.disconnect / removeEventListener on unmount |
| none | `frontend/components/ui/info-card.tsx` | InfoCard onMouseMove -> --rotation style write (lines 28-37) — 6 cards on '/' | pointer event handler (not a loop) | Per mousemove over a card (~60-120 Hz while hovering) | getBoundingClientRect (layout read) + atan2 + style.setProperty (attribute mutation -> triggers the chat-widget MO) | n/a | React synthetic handler, none needed |
| none | `frontend/components/chat/chat-widget.tsx` | ChatWidget wake listeners (:74-76) + 20 s backstop setTimeout (:82) | one-shot listeners ({once:true}) + setTimeout | Once | setWake(true) -> injects widget.js | Backstop armed only if document.visibilityState === 'visible' | removeEventListener + clearTimeout on deps change/unmount |
| none | `index.js` | widget.js themeObserver MutationObserver(documentElement class) (:249-256); toggleChat setTimeouts (:142 10 ms, :163 400 ms); message/metnma | MutationObserver (attributeFilter ['class'] on <html>) + one-shot setTimeouts + event listeners | On theme toggle / on chat open-close only | iframeContainer.style.backgroundColor write + postMessage (the style write is a body-subtree attribute mutation that feeds the HOST observer | No | None (third-party script lives for the page) |
| none | `frontend/lib/analytics/collector.ts` | scroll -> requestAnimationFrame(sampleScroll) (:169-173); scheduleFlush setTimeout(5000) (:118-124); pagehide/visibilitychange/click/focusin | rAF throttle (one per frame while scrolling) + one-shot flush timer + delegated listeners | rAF only during scroll events; flush at most once per 5 s while events are queued | read scrollHeight/innerHeight/scrollY (layout read, no write); sendBeacon/fetch keepalive | No (flush on hidden via sendBeacon) | Module-level singleton; listeners live for the page |
| none | `frontend/lib/analytics/provider.tsx` | AnalyticsProvider setTimeout(300) page_view (:27-40) | setTimeout one-shot | Once per pathname / consent change | querySelector meta + getTracker().pageView | No | clearTimeout on deps change/unmount |
| none | `frontend/components/layout/route-progress.tsx` | RouteProgress showTimer (120 ms) / safetyTimer (12 s) (:48-73) | setTimeout one-shots armed by capture-phase click | Per qualifying link click | setActive(true|false) | No | clearTimeout on pathname commit, pageshow, unmount |
| none | `frontend/components/commerce/cart-toast.tsx` | CartToast setTimeout(5000) (:17-28) | setTimeout one-shot | Once per cart removal | setVisible(false) + dismissRemoved() | No | clearTimeout on deps change/unmount |
| none | `frontend/components/commerce/currency-provider.tsx` | CurrencyProvider layout effect + /api/geo fetch (:101-176) | one-shot effect with cancellable fetch | Once per mount | cookie/storage reads, optional fetch | No | cancelled flag on unmount |
| none | `frontend/components/commerce/store-provider.tsx` | StoreProvider localStorage hydrate/persist effects (:49-77) | one-shot / deps-driven effects | On mount and on cart/wishlist change | JSON parse/stringify of the cart | No | n/a |
| none | `frontend/components/commerce/account-menu.tsx` | AccountMenu /api/account/me fetch (:55-73) + mm:avatar-updated listener (:75-83) | one-shot fetch + event listener | Once per mount | setMe/setLoaded | No | cancelled flag; removeEventListener |
| none | `frontend/components/{commerce/cart-rail.tsx, commerce/departments-menu.tsx, layout/mobile-nav.tsx, legal/consent-banner.tsx, ui/use-dialog.t` | Overlay/header clients: pointerdown-capture, keydown, mousedown listeners (only while open); search-bar input debounces; theme-toggle localS | event listeners / input-debounce setTimeouts | Event-driven only; no periodic work | close-on-outside-click / focus trap / debounce fetch | n/a | removeEventListener / clearTimeout in effect cleanups |
| none | `app/globals.css` | CSS animations on '/': marquee (trusted-by, 70 s), scroll-y x2 (product mosaic, 26 s), carousel-progress (5.5 s), route-progress (8 s, on cl | CSS keyframes (compositor) | Continuous (marquee, scroll-y) — transform-only, off main thread | Compositor transform; no JS, no layout | Browser pauses compositor animations in hidden tabs; group-hover pause; motion-reduce:animate-none | n/a |
| none | `frontend/components/ui/vapour-text-effect.tsx` | useIsInView IntersectionObserver (x6) | IntersectionObserver (:774-781) | on viewport crossing only | setIsInView -> re-render -> loop effect restart; setTimeout(0) -> setAnimationState('vaporizing') (:153-155) | n/a (it IS the gate) | observer.disconnect() (:783-785) |
| none | `frontend/components/ui/vapour-text-effect.tsx` | handleFontChange 1 s re-sample timer (x6) | setTimeout(1000) (:412-423) | scheduled once at mount, always cleared by the wrapperSize re-run before it fires (dead path) | would be cleanup + renderCanvas (one getImageData) | no | clearTimeout in effect cleanup (:425-428) |
| none | `frontend/components/home/hero-stats.tsx` | useDisplayStyle ResizeObserver + MutationObserver (x2: numRef, labelRef) | ResizeObserver on hidden reference span (:81-82) + MutationObserver on <html> class attribute (:83-84) | RO: once + on font-load/breakpoint; MO: only theme toggle (theme-toggle.tsx:39 is the sole writer) | getComputedStyle + nextDisplayStyle identity check; re-render only if a value actually changed | no | ro.disconnect(); mo.disconnect() (:85-88) |
| none | `frontend/components/home/hero-stats.tsx` | static-cycle setInterval (StaticSlot path only) | setInterval 4000 ms (:189-193) | every 4 s, only when reduced-motion or <640px AND n > 3 | setStep -> 3 StaticSlot re-renders (text only) | no | clearInterval (:192) |
| none | `frontend/components/home/featured-projects-carousel.tsx` | autoplay setTimeout keyed on active [outside vector] | setTimeout 5500 ms (:83-87) + IntersectionObserver threshold 0.35 (:60-62) + visibilitychange | every 5.5 s while in view, visible, not hovered, no reduced-motion | setActive -> CSS opacity cross-fade | yes (IO + document.hidden) | clearTimeout / io.disconnect / removeEventListener |
| none | `frontend/lib/analytics/collector.ts` | onScroll -> requestAnimationFrame(sampleScroll) [outside vector] | one-shot rAF throttle per scroll event (:166-172), not self-rescheduling | <= 1 per frame while scrolling, 0 idle | reads scrollHeight/innerHeight/scrollY (one forced layout at most) | n/a | n/a (module-level, no loop) |
| none | `frontend/components/home/product-mosaic.tsx` | animate-scroll-y CSS keyframes x2 columns (+ marquee / logo-cycle keyframes elsewhere on the page) [outside vector] | CSS transform animation (tailwind.config.ts:71-74, 98) under a mask-image | 26 s linear infinite, compositor thread | compositor-only transform; no main-thread JS | browser-managed | n/a |
| none | `frontend/components/home/hero-stats.tsx` | useDisplayStyle (x2): ResizeObserver on hidden reference span + MutationObserver on <html> class | ResizeObserver + MutationObserver(attributeFilter:['class']) | On breakpoint font-size change or theme toggle only | getComputedStyle read of 4 properties + nextDisplayStyle identity check | No (but effectively idle) | both disconnected on unmount |
| none | `frontend/components/home/hero-stats.tsx` | setInterval(setStep, 4000) when cycleStatic | setInterval | Every 4 s, phones/reduced-motion only (no canvases mounted in that branch) | one setState -> re-render 3 StaticSlots | No | clearInterval on deps change/unmount |
| none | `frontend/components/home/featured-projects-carousel.tsx` | setTimeout(next, 5500) keyed on active | setTimeout chain | Every 5.5 s while autoplaying | setActive -> re-render 15 mounted <article> slides (class swap) + CSS opacity transition 700 ms; progress bar CSS scaleX | Yes: IntersectionObserver threshold 0.35, document.hidden, hover/focus, prefers-reduced-motion | clearTimeout on deps change/unmount; IO and visibilitychange listeners removed |
| none | `frontend/lib/analytics/collector.ts` | onScroll -> requestAnimationFrame(sampleScroll); scheduleFlush 5 s | scroll listener (passive) throttled to one rAF; one-shot timeout | At most once per frame while the user scrolls; flush every 5 s only when the queue is non-empty | reads scrollHeight/innerHeight/scrollY (one layout read) and updates maxScroll; flush = JSON.stringify + sendBeacon/fetch | visibilitychange triggers a beacon flush; scroll sampling naturally stops in background | Listeners are registered once per memoised instance and never removed (module-level); duplicates after resetTracker collapse via the shared  |
| none | `frontend/components/chat/chat-widget.tsx` | wake listeners (pointerdown/pointermove/keydown/touchstart, once) + 20 s backstop timeout | one-shot listeners + setTimeout | Once | setWake(true) -> inject widget.js | Backstop timeout only armed when document.visibilityState === 'visible' | listeners removed and timeout cleared in effect cleanup |
| none | `frontend/components/ui/vapour-text-effect.tsx` | useIsInView IntersectionObserver (x6) | IntersectionObserver | On intersection change only | setIsInView(boolean) (bails out when unchanged) | n/a | disconnect on unmount |
| none | `frontend/components/ui/info-card.tsx (x6 in services-preview)` | onMouseMove -> getBoundingClientRect + style.setProperty('--rotation') | pointer event | Per mousemove over a card | one layout read + one inline style write (which, at HEAD, also fires the body-wide chat-widget MutationObserver) | n/a | React-managed handler |
| none | `tailwind.config / app/globals.css (marquee 70 s, scroll-y 26 s x2 under mask-image, carousel-progress scaleX, badge-pop, rise-in, fade-up)` | CSS keyframe animations | CSS animation (transform/opacity only) | Continuous (marquee, scroll-y infinite) | Compositor-driven transform; no main-thread JS | Browser pauses CSS animations in hidden tabs; group-hover paused; motion-reduce:animate-none | n/a |
| none | `frontend/lib/analytics/provider.tsx` | setTimeout(pageView, 300) per pathname/consentTick | one-shot setTimeout | Once per navigation or consent change | querySelector meta + getTracker().pageView | n/a | clearTimeout in effect cleanup |
| none | `frontend/components/chat/chat-widget.tsx` | WAKE_EVENTS pointerdown/pointermove/keydown/touchstart (75) + CONSENT_EVENT (66) + 20 s backstop timer (82) | addEventListener {once, passive} / custom event / setTimeout | Once | setWake(true) | Backstop timer only set when document.visibilityState === 'visible' | All removed / cleared in effect cleanup |
| none | `frontend/components/home/hero-stats.tsx` | useDisplayStyle: ResizeObserver(hidden span) + MutationObserver(<html>, attributeFilter:['class']) — 2 instances (81-88) | ResizeObserver + MutationObserver | Only on a real size change of the hidden reference span (breakpoint / font load) or an <html> class change (theme toggle) | getComputedStyle(el) + identity-preserving setStyle (nextDisplayStyle) | n/a (event-driven, no per-frame work) | ro.disconnect() + mo.disconnect() |
| none | `frontend/components/home/hero-stats.tsx` | useMediaQuery x2 (97-101); cycleStatic setInterval 4000 ms (191, phones/reduced-motion only) | matchMedia change / setInterval | On media change / every 4 s on the static fallback only | setState | No (trivial) | removeEventListener / clearInterval |
| none | `frontend/components/ui/vapour-text-effect.tsx` | useIsInView IntersectionObserver — 6 instances (774-786); handleFontChange 1 s setTimeout (412-423) | IntersectionObserver / setTimeout | On intersection change / once per font change | setIsInView / one renderCanvas | n/a | disconnect / clearTimeout |
| none | `frontend/components/home/featured-projects-carousel.tsx` | matchMedia change (51), IntersectionObserver threshold 0.35 (60), document visibilitychange (71), autoplay setTimeout 5500 ms (83) | listeners + IntersectionObserver + setTimeout | Event-driven; one timeout per slide while autoplaying | setState; slide change is a CSS opacity transition | Yes: inView && pageVisible && !hoverPaused && !reduceMotion | All removed / disconnected / cleared |
| none | `frontend/components/ui/animated-text-cycle.tsx` | setInterval word cycler — 2 instances on the homepage (35-38) | setInterval | Every 2600 ms | setIndex → one framer-motion blur/opacity transition (0.4 s in / 0.3 s out) | No | clearInterval |
| none | `frontend/components/ui/info-card.tsx` | onMouseMove cursor-tracking conic border (28-37) — 3-4 cards | React synthetic mousemove (only while hovering a card) | Per mousemove over a card | getBoundingClientRect + style.setProperty('--rotation') (paint-only; no layout write → no thrash) | n/a | React-managed |
| none | `frontend/lib/analytics/collector.ts` | scroll {passive} + rAF-throttled sampleScroll (169-173, 275); click capture passive (278); focusin capture passive (308); pagehide (264); vi | addEventListener (module singleton, registered once per page lifetime) | Per scroll gesture (≤1 sample per frame) / per click / per focus | Reads scrollHeight, innerHeight, scrollY only; closest() + push() on click | visibilitychange flushes on hidden | Intentionally never removed (singleton, guarded by `instance`); cannot register twice |
| none | `frontend/components/layout/route-progress.tsx` | document click (capture) + window pageshow (63-66) | addEventListener | Per click | targetPathname() pure check + 2 setTimeouts | n/a | Removed + timers cleared in effect cleanup |
| none | `frontend/components/ui/use-dialog.ts (QuoteDrawer, QuoteModal); layout/mobile-nav.tsx; commerce/departments-menu.tsx; commerce/cart-rail.tsx` | keydown (and capture-phase pointerdown for departments-menu / cart-rail) registered only while the overlay is open | addEventListener, scoped to `open` | Per keypress / pointerdown while open; zero listeners while closed | contains() check; on Tab only: querySelectorAll + offsetParent/getClientRects reads; body.style.overflow written once per open/close | n/a | Removed in effect cleanup on close/unmount |
| none | `frontend/components/commerce/search-bar.tsx; commerce/account-menu.tsx; commerce/chat-cart-bridge.tsx; lib/analytics/provider.tsx; legal/con` | document mousedown (search-bar 166); mm:avatar-updated (account-menu 80); metnmat:add-to-cart (chat-cart-bridge 42); CONSENT_EVENT (provider | addEventListener (custom events / mousedown) | Event-driven | setState / contains() / one fetch | n/a | Removed in effect cleanup |
| none | `Metnmat-customer-agent-main/iframe-chat-widget/widget/dist/widget.js (third-party, served as https://chat.metnmat.com/widget.js)` | MutationObserver(<html>, attributeFilter:['class']); window 'message'; window 'load'; window 'metnmat:cart-result'; launcher onmouseover/onm | MutationObserver + listeners (host document) | Per theme toggle / per postMessage / per hover | c.style.backgroundColor write + postMessage (theme); inline style writes on hover — each of which re-fires the HOST body observer above (har | No | Never (page lifetime, by design) |
| none | `frontend/components/chat/chat-widget.tsx` | useEffect [wake, decided] — WAKE_EVENTS listeners + 20 s backstop setTimeout | once:true listeners + one-shot timer | Fires once | setWake(true) | Timer only armed when document.visibilityState==='visible' | removeEventListener ×4 + clearTimeout in cleanup |
| none | `frontend/components/chat/chat-widget.tsx` | useEffect [] — CONSENT_EVENT listener (check) | event listener | Per consent change | readConsent() (getItem + JSON.parse) + boolean setState | N/A | removeEventListener |
| none | `https://chat.metnmat.com/widget.js (live; local dist at C:/Users/ritik/OneDrive/Desktop/Metnmat-customer-agent-main/iframe-chat-widget/widge` | themeObserver on document.documentElement {attributes, attributeFilter:['class']} | MutationObserver | Only when <html class> changes (theme toggle) | getSiteTheme() (classList + localStorage read), one inline style write on the panel, one postMessage | No | None (lives for the page); cannot self-trigger — it never writes <html class> |
| none | `https://chat.metnmat.com/widget.js (live)` | window 'message' handler, document 'keydown' (Escape), window 'metnmat:cart-result' | event listeners | User/iframe action only | origin+source check, one postMessage or one CustomEvent dispatch or location.assign | N/A | None (page lifetime) |
| none | `https://chat.metnmat.com/widget.js (live)` | toggle h(): setTimeout 10 ms / 400 ms | one-shot timers | Per bubble click | 3-4 inline style writes, innerHTML swap, focus() | N/A | Self-expiring |
| none | `frontend/lib/analytics/collector.ts` | scheduleFlush → setTimeout(send, 5000) | one-shot timer, re-armed only by a new event | At most one pending at a time; only while events exist | JSON.stringify of ≤25 events + fetch keepalive | No (but visibilitychange→hidden flushes immediately via sendBeacon) | clearTimeout in resetTracker / pagehide |
| none | `frontend/lib/analytics/provider.tsx` | useEffect [pathname, consentTick] — setTimeout(pageView, 300) | one-shot timer | Once per navigation / consent change | one querySelector + getTracker().pageView | No | clearTimeout on re-run/unmount |
| none | `frontend/components/legal/consent-banner.tsx` | OPEN_CONSENT_EVENT listener; keydown focus trap while the preferences dialog is open | event listeners | Per event; keydown handler exists only while showPrefs | Tab: querySelectorAll on the dialog + offsetParent filter; Escape: two setStates | N/A | Both removed in effect cleanup |
| none | `frontend/components/commerce/currency-provider.tsx` | useIsomorphicLayoutEffect [] — cookie/session/legacy resolution, optional fetch('/api/geo') | one-shot layout effect | Once per mount (root layout persists across navigations) | 2 removeItem, URLSearchParams, sessionStorage read, cookie regex, ≤1 JSON.parse, ≤1 fetch | N/A | cancelled flag for the fetch |
| none | `app/layout.tsx` | themeScript (inline <script> in <head>) | one-shot synchronous script | Once per document | localStorage.getItem + classList.add | N/A | N/A |
| none | `instrumentation.ts` | register() | server boot hook | Once, server only (returns unless NEXT_RUNTIME==='nodejs') | env checks | N/A | N/A |
| none | `frontend/components/commerce/chat-cart-bridge.tsx` | metnmat:add-to-cart listener | event listener | Per chat 'Add to cart' tap | one fetch + addToCart + one CustomEvent | N/A | removeEventListener |
| none | `frontend/components/layout/route-progress.tsx` | document click (capture) + pageshow listeners; SHOW_AFTER_MS / SAFETY_MS timers | event listeners + one-shot timers | Per link click | targetPathname() parse + 2 setTimeout | N/A | Both listeners removed, both timers cleared |
| none | `frontend/components/commerce/store-provider.tsx` | mount read effect; cart/wishlist persistence effects | one-shot + change-driven effects | Once on mount; per cart/wishlist change | JSON.parse ×2 on mount; JSON.stringify + setItem per change | N/A | N/A |
| none | `frontend/components/home/hero-stats.tsx` | useDisplayStyle: ResizeObserver(reference span) + MutationObserver(html class) (x2) | observers, read-only callback | Once on observe; then on breakpoint or theme toggle | getComputedStyle + identity-preserving setState | No | ro.disconnect(); mo.disconnect() |
| none | `frontend/components/home/hero-stats.tsx` | cycleStatic setInterval(4000) (phones / reduced-motion only) | setInterval | 0.25 Hz, only when canvases are not used | setStep | No | clearInterval |
| none | `frontend/components/home/featured-projects-carousel.tsx` | autoplay setTimeout(AUTOPLAY_MS=5500) keyed on active; IntersectionObserver; visibilitychange; matchMedia | one-shot timer re-armed per slide + observers | 0.18 Hz | setActive -> opacity class swap across 15 slides | Yes: inView && pageVisible && !hoverPaused && !reduceMotion | clearTimeout; io.disconnect(); removeEventListener x2 |
| none | `frontend/components/home/product-mosaic.tsx / ui/marquee.tsx / carousel progress` | CSS animations scroll-y (26 s, x2 columns), marquee (70 s), carousel-progress (5.5 s) | CSS transform animation (compositor) | continuous, off main thread | compositor transform | Browser-managed | n/a (DOM removal) |
| none | `frontend/components/layout/route-progress.tsx` | document click (capture) + window pageshow listeners; 120 ms / 12 s timers | event listeners + one-shot timers | per click | closest('a') + URL parse | No | removeEventListener x2 + clearTimeout x2 in cleanup; stop() on pathname change |
| none | `frontend/lib/analytics/provider.tsx` | CONSENT_EVENT listener; 300 ms page_view timeout per pathname | listener + one-shot timer | per navigation | querySelector meta + getTracker().pageView | No | removeEventListener; clearTimeout |
| none | `index.js (runs in host page after wake)` | window 'message' listener, 'metnmat:cart-result' listener, themeObserver MutationObserver(html class, attributeFilter) | event-driven listeners + observer | per chat event / per theme toggle | postMessage / inline style write on the panel | No | never (page-lifetime, script injected once) |
| none | `frontend/components/commerce/chat-cart-bridge.tsx, legal/consent-banner.tsx, commerce/cart-rail.tsx, commerce/cart-toast.tsx, commerce/accou` | assorted window/document listeners and timers | event listeners / one-shot timers, mostly armed only while an overlay is open | per interaction | trivial | n/a | every one has a matching removeEventListener/clearTimeout in its effect cleanup (verified line by line) |
| none | `frontend/components/home/featured-projects-carousel.tsx` | autoplay setTimeout(next, 5500) (lines 81-85) + carousel-progress CSS keyframe | setTimeout re-armed on `active` + CSS animation | Every 5.5 s | setActive -> toggles opacity classes on N stacked <article>s (700 ms opacity transition); progress bar scaleX on the compositor | Yes: IntersectionObserver threshold 0.35, visibilitychange, hover/focus pause, reduced-motion off | clearTimeout / io.disconnect / removeEventListener (64, 72, 84) |
| none | `frontend/components/ui/marquee.tsx` | animate-marquee CSS transform keyframe (line 20), durationSec=70 from trusted-by.tsx | CSS animation (transform, will-change:transform) | Continuous, compositor thread | Compositor transform of a ~14.4k px wide tiled layer (62 cards); ~1 new tile column rasterized per ~1.25 s | Browser-native (offscreen animations throttled); paused on hover | n/a |
| none | `frontend/components/home/product-mosaic.tsx` | animate-scroll-y CSS transform keyframe (line 249), two columns, 26 s | CSS animation (transform) | Continuous, compositor thread (lg+ only) | Compositor translateY of two card columns (8 cards each incl. duplicates) under a parent mask-image | Browser-native; paused on group hover | n/a |
| none | `frontend/lib/analytics/collector.ts` | onScroll -> rAF sampleScroll (lines 150-175, 275) | scroll listener throttled to one rAF | Once per frame during scrolling only | Reads scrollHeight, innerHeight, scrollY; no writes | n/a | Page-lifetime listener by design |
| none | `frontend/components/chat/chat-widget.tsx` | WAKE_EVENTS once-listeners + 20 s backstop timer (lines 70-88) | one-shot listeners / setTimeout | Once | setWake(true) -> injects widget.js | Timer only armed when document.visibilityState === "visible" | removeEventListener + clearTimeout (84-87) |
| none | `frontend/components/legal/consent-banner.tsx` | preferences-dialog scroll lock effect (lines ~118-175) | effect on dialog open | Once per dialog open | One forced layout read (innerWidth - documentElement.clientWidth) then body overflow/padding writes; keydown focus trap | n/a | Restores body styles, removes keydown listener |
| none | `frontend/components/chat/chat-widget.tsx (HEAD d52fd4e)` | outer/inner MutationObservers (post-fix) | MutationObserver: body childList-only until the widget exists, then panel {attributes, attributeFilter:["style"]} + container {childList} | only on widget open/close style writes and container child changes | snapshot (2 getElementById, 2 querySelector, 1 getComputedStyle) + value-guarded writes (planA11yWrites returns [] when settled) | n/a — event-driven | outer.disconnect(); inner?.disconnect() on unmount / wake change |
| none | `frontend/components/home/hero-stats.tsx` | useDisplayStyle (numRef, labelRef) | ResizeObserver on the hidden reference span + MutationObserver on <html> class (attributeFilter) | on font-swap/breakpoint resize of the span, and on theme toggle | getComputedStyle + nextDisplayStyle identity bail-out (stable-updates.ts:70-84) | n/a | ro.disconnect(); mo.disconnect() (hero-stats.tsx:85-88) |
| none | `frontend/components/home/hero-stats.tsx` | useMediaQuery ×2, static-cycle setInterval | matchMedia change listeners; setInterval(4000) only when reduce||small (phones) | 4 s (phones/reduced-motion only) | one setStep → 3 StaticSlot re-renders | no (but trivial) | clearInterval / removeEventListener |
| none | `frontend/components/ui/vapour-text-effect.tsx (×6 on desktop)` | renderCanvas effect (lines 289-312) | useEffect keyed on texts/font/color/alignment/wrapperSize/currentTextIndex — deps are identity-stable (useMemo in VaporSlot, ANIM const, nex | once after initial size, then once per text change (~5.4 s) | same as above: one getImageData + particle rebuild | indirectly — text changes only advance while isInView and !document.hidden | handleFontChange cleanup clears its 1 s timeout and particles |
| none | `frontend/components/home/featured-projects-carousel.tsx` | autoplay setTimeout(next, 5500) + IntersectionObserver + visibilitychange + matchMedia | per-slide setTimeout keyed on `active` | 5.5 s | setActive → re-render of N slides (class toggle, CSS opacity transition) | yes — inView (threshold 0.35), pageVisible, hoverPaused, reduceMotion | clearTimeout / io.disconnect / removeEventListener |
| none | `frontend/lib/analytics/collector.ts` | getTracker() listeners (scroll/click/focusin/pagehide/visibilitychange) + scheduleFlush | passive event listeners; scroll sampling rAF-throttled; 5 s flush timer | ≤1 layout read per frame while scrolling; flush every 5 s only while events are queued | scrollHeight/innerHeight read + one sendBeacon/fetch per flush | flush-on-hidden via sendBeacon | listeners persist for the page lifetime by design (never re-registered: instance memoised) |
| none | `frontend/components/chat/chat-widget.tsx` | wake listeners + 20 s backstop timer | once-listeners (pointerdown/pointermove/keydown/touchstart) + single setTimeout | one-shot | setWake(true) → script injection | timer only armed when visibilityState === 'visible' | removeEventListener / clearTimeout |
| none | `Metnmat-customer-agent-main/iframe-chat-widget/widget/dist/widget.js` | widget's own MutationObserver on document.documentElement class | MutationObserver {attributes, attributeFilter:['class']} → sets panel backgroundColor + postMessage | theme toggles only | one style write + one postMessage | n/a | none (lives with the page) |
| none | `frontend/components/commerce/currency-provider.tsx` | useIsomorphicLayoutEffect region resolution | one-shot layout effect (+ one fetch('/api/geo') on first visit) | once per page load | cookie/sessionStorage read; possible one synchronous provider-wide re-render before first paint | n/a | cancelled flag |
| none | `frontend/components/ui/info-card.tsx (×6)` | onMouseMove handler | pointer-driven (hover only) | per mousemove over a card (~60-120 Hz while hovering) | getBoundingClientRect + CSS custom-property write → conic-gradient repaint of one ~370×300 card | n/a | n/a |
