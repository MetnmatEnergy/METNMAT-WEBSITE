# Forensic audit — confirmed findings register

Generated 2026-09-02 by a 132-agent read-only audit across 11 dimensions.
121 findings were raised; each was then handed to independent verifiers
prompted to REFUTE it. **71 survived, 50 were refuted** and are not listed.

A row here means the finding was reproduced against real code, not inferred.
`EXTERNAL` marks one blocked on infrastructure or another repository.

## Fixed since the audit

- **Unauthenticated IDOR in `/api/quote`** (critical) and the three highs that
  shared its input — unencoded id interpolation into an internal-key URL, and
  the unbounded/undeduped/sequential readback. Fixed in `350b6fc`.

## Register

### Critical (2)

| Dimension | Finding | Location |
|---|---|---|
| security | Unauthenticated IDOR in /api/quote exfiltrates any customer's RFQ attachment by email | `apps/website/src/app/api/quote/route.ts:98` |
| security | CMS staff sign-in is a 4-digit PIN guarded only by an in-process per-IP counter | `apps/dashboard/src/app/pin-login/route.ts:68` |

### High (7)

| Dimension | Finding | Location |
|---|---|---|
| perf-rendering | CMS fetches have no timeout, so a slow CMS blocks the render indefinitely | `apps/website/src/frontend/lib/blog.ts:20` |
| scroll-drawer | html { overflow-x: clip } makes every body scroll lock a no-op AND un-sticks the site header | `apps/website/src/app/globals.css:88` |
| forms-inventory | Checkout Pay button locks forever: loadRazorpay() promise can never settle on a retry | `apps/website/src/app/checkout/page.tsx:30` |
| customization-email | Any anonymous caller can have another customer's private uploaded file emailed to an address they choose | `apps/website/src/app/api/quote/route.ts:117` |
| customization-email | Attachment readback is unbounded and undeduped, and runs sequentially inside the request | `apps/website/src/app/api/quote/route.ts:98` |
| security | Unvalidated attachment id is interpolated unencoded into the CMS URL, giving an internal-key-authenticated request primitive | `apps/website/src/backend/services/enquiries.service.ts:82` |
| api-db | Unbounded client-supplied attachment id list drives a sequential double-round-trip loop on the public /api/quote endpoint | `apps/website/src/app/api/quote/route.ts:117` |

### Medium (30)

| Dimension | Finding | Location |
|---|---|---|
| perf-rendering | three.js is statically imported into the /about route bundle | `apps/website/src/frontend/components/ui/animated-shader-background.tsx:4` |
| perf-client | No loading.tsx on the dynamic content routes and no global route-transition indicator — every nav click is visually dead for the whole server render | `apps/website/src/frontend/components/layout/nav-links.tsx:21` |
| perf-client | Mobile nav drawer stays open over a scroll-locked page for the entire navigation | `apps/website/src/frontend/components/layout/mobile-nav.tsx:21` |
| perf-client | /blog toolbar navigates with a bare router.push and no useTransition — the correct pattern exists in-repo but is not used here | `apps/website/src/frontend/components/blog/blog-toolbar.tsx:48` |
| perf-client | loadRazorpay() can return a promise that never resolves, permanently freezing the Pay button | `apps/website/src/app/checkout/page.tsx:33` |
| scroll-drawer | MobileNav backdrop is trapped inside the header's backdrop-filter containing block and renders 0px tall — no scrim, no click-to-close | `apps/website/src/frontend/components/layout/mobile-nav.tsx:125` |
| scroll-drawer | Page scrolls freely behind every hand-rolled modal and drawer | `apps/website/src/frontend/components/commerce/quote-modal.tsx:53` |
| forms-inventory | /blog search and filter form produces no pending feedback at all on a fully-dynamic route | `apps/website/src/frontend/components/blog/blog-toolbar.tsx:45` |
| forms-inventory | Quote drawer and quote modal discard the server's error message, including 429 rate-limits | `apps/website/src/frontend/components/commerce/quote-drawer.tsx:128` |
| forms-inventory | Quote overlays submit with failed attachments silently dropped, then show an unqualified success screen | `apps/website/src/frontend/components/commerce/quote-drawer.tsx:68` |
| customization-email | Both emails omit `message`, so a /quote-page submission notifies the team with only a contact card and none of the request | `apps/website/src/backend/lib/email.ts:36` |
| customization-email | No reference ID is generated, returned, emailed, or shown — the customer has nothing to quote back and staff have nothing to search on | `apps/website/src/backend/services/enquiries.service.ts:129` |
| customization-email | A failed CMS save returns 201 with no alert, leaving the RFQ in an inbox only — invisible in the admin queue and in the customer's account | `apps/website/src/app/api/quote/route.ts:141` |
| customization-email | The upload rate limit equals the maximum number of attachments, so a single failed upload cannot be retried | `apps/website/src/app/api/quote/upload/route.ts:17` |
| customization-email | No length cap on any enquiry field, though the sibling validator caps its free text for exactly this reason | `apps/website/src/backend/validation/index.ts:34` |
| routing-nav | "Continue with Google" is rendered unconditionally, but Google OAuth is unconfigured in production — every click is a guaranteed dead end | `apps/website/src/frontend/components/commerce/auth-card.tsx:344` |
| routing-nav | Every listing route that reads searchParams is fully uncached, not just /blog — including the shop category pages | `apps/website/src/app/shop/c/[category]/page.tsx:1` |
| routing-nav | Site search tokenizer splits on the literal letter "s" instead of whitespace | `apps/website/src/frontend/lib/cms.ts:329` |
| routing-nav | The invoice route builds its login redirect from req.url instead of the canonical siteBase(), unlike every other auth redirect | `apps/website/src/app/api/orders/[orderNumber]/invoice/route.ts:222` |
| security | admin.metnmat.com serves no security headers — no CSP, nosniff, frame-ancestors, Referrer-Policy or HSTS | `apps/dashboard/next.config.mjs:7` |
| security | Support ticket access credential has only ~65k of entropy per day | `apps/website/src/app/api/support/route.ts:72` |
| security | Rate limiting silently degrades to a per-process in-memory map that resets on every restart **(EXTERNAL)** | `apps/website/src/backend/lib/rate-limit.ts:64` |
| api-db | The primary CMS fetch wrapper has no timeout — a slow upstream stalls every page render unbounded | `apps/website/src/frontend/lib/cms.ts:32` |
| api-db | Missing backslash in the searchSite tokenizer — splits on the letter "s" instead of whitespace | `apps/website/src/frontend/lib/cms.ts:329` |
| a11y-mobile | Theme toggle is completely inoperable by keyboard and invisible to screen readers | `apps/website/src/app/globals.css:376` |
| a11y-mobile | Quote-form validation errors are not programmatically associated with their fields | `apps/website/src/frontend/components/commerce/quote-drawer.tsx:215` |
| a11y-mobile | Chat bubble covers the cart toast's Undo button on phone widths | `apps/website/src/frontend/components/commerce/cart-toast.tsx:37` |
| a11y-mobile | Three confirmation/notice overlays render as plain divs with no dialog semantics, Escape, or focus handling | `apps/website/src/frontend/components/commerce/quote-drawer.tsx:376` |
| cms-chatbot | Chat iframe downloads 385 KB on first pointer move, not on open — the widget's own deferral is undone by an eagerly-assigned iframe src | `iframe-chat-widget/widget/src/index.js:115` |
| cms-chatbot | Every cache-revalidation failure is silent on both sides — a 401, a missing key, or a wrong WEBSITE_URL all look identical to 'it worked' | `apps/dashboard/src/hooks/revalidate.ts:26` |

### Low (32)

| Dimension | Finding | Location |
|---|---|---|
| perf-rendering | Minute-floored timestamp in the query URL defeats stale-while-revalidate on every blog query | `apps/website/src/frontend/lib/blog.ts:71` |
| perf-rendering | framer-motion ships to the homepage for a rotating text badge | `apps/website/src/frontend/components/ui/animated-text-cycle.tsx:5` |
| perf-rendering | Verified as fine — explicitly not causes of the slowness | `apps/website/src/frontend/components/chat/chat-widget.tsx:47` |
| perf-client | Checkout Pay is a serial waterfall: create-order round-trip, then a cold-origin download of Razorpay checkout.js | `apps/website/src/app/checkout/page.tsx:557` |
| perf-client | Account sidebar "Sign out" awaits the network with no pending state at all | `apps/website/src/frontend/components/commerce/account-nav.tsx:21` |
| scroll-drawer | Account dropdown is a Radix MODAL, so it sets body{pointer-events:none} and eats the first click on Get a Quote / nav links | `apps/website/src/frontend/components/commerce/account-menu.tsx:99` |
| scroll-drawer | Product-image lightbox applies no scroll lock at all | `apps/website/src/frontend/components/commerce/product-gallery.tsx:299` |
| scroll-drawer | Mobile menu stays open (and locked) when the tapped link is the current pathname | `apps/website/src/frontend/components/layout/mobile-nav.tsx:22` |
| forms-inventory | No client-side form submit anywhere has a timeout — a hung origin spins the button forever | `apps/website/src/app/checkout/page.tsx:478` |
| forms-inventory | /quote page form has no client-side validation of any kind | `apps/website/src/frontend/components/commerce/quote-form.tsx:88` |
| forms-inventory | Sign-out in the account sidebar has no disabled state, spinner, or in-flight guard | `apps/website/src/frontend/components/commerce/account-nav.tsx:21` |
| forms-inventory | Forgot-password form never reads the response body, so a rate-limit reads as "Something went wrong" | `apps/website/src/app/forgot/page.tsx:28` |
| forms-inventory | Address delete and set-default fire immediately with no confirmation and no per-row progress | `apps/website/src/frontend/components/commerce/address-book.tsx:437` |
| customization-email | No idempotency guard anywhere in the quote path — a retry creates a duplicate RFQ and re-sends both emails with attachments | `apps/website/src/app/api/quote/route.ts:136` |
| customization-email | The success UI tells the customer a copy was emailed to them when only the internal team email succeeded | `apps/website/src/backend/lib/email.ts:159` |
| customization-email | The customization drawer has no honeypot field, unlike every other public form on the site | `apps/website/src/frontend/components/commerce/quote-drawer.tsx:113` |
| oauth-chain | OAuth handshake cookie cleanup is a silent no-op in production (__Host- prefix cleared without Secure) | `apps/website/src/app/api/account/google/callback/route.ts:34` |
| routing-nav | Search typeahead results are <button onClick={router.push}> instead of links — no middle-click, no new tab, no copyable URL | `apps/website/src/frontend/components/commerce/search-bar.tsx:334` |
| routing-nav | CMS-editable header/footer nav hrefs are unvalidated free text fed straight into <Link href> on every page | `apps/dashboard/src/globals/index.ts:337` |
| routing-nav | One global 404 page with no segment-level variants, hardcoded to a white background, offering only "Go to Home" | `apps/website/src/frontend/components/ui/404-page-not-found.tsx:12` |
| security | Production CSP allows script-src 'unsafe-inline', neutralising it as an XSS backstop | `apps/website/next.config.mjs:52` |
| security | .gitignore does not cover .env.production or .env.development | `.gitignore:15` |
| api-db | Checkout re-fetches every cart item one at a time before creating the payment order | `apps/website/src/app/api/checkout/create-order/route.ts:127` |
| api-db | Analytics beacon blocks on the CMS hop, contradicting the comment directly above it | `apps/website/src/app/api/a/collect/route.ts:143` |
| a11y-mobile | Chat widget iframe has no title attribute **(EXTERNAL)** | `Metnmat-customer-agent-main/iframe-chat-widget/widget/src/index.js:114` |
| a11y-mobile | Chat launcher's accessible name never changes when it becomes a close button, and it exposes no expanded state **(EXTERNAL)** | `Metnmat-customer-agent-main/iframe-chat-widget/widget/src/index.js:68` |
| a11y-mobile | Mobile filter drawer declares role="dialog" aria-modal but does no focus management at all | `apps/website/src/frontend/components/commerce/filter-drawer.tsx:21` |
| a11y-mobile | QuoteModal has no focus trap and does not restore focus to its trigger | `apps/website/src/frontend/components/commerce/quote-modal.tsx:44` |
| a11y-mobile | Product size selector uses role="radio" without roving tabindex or arrow-key navigation | `apps/website/src/frontend/components/commerce/product-buy-box.tsx:45` |
| a11y-mobile | Cart toast creates its live region at the same moment as its content, so the announcement may be dropped | `apps/website/src/frontend/components/commerce/cart-toast.tsx:30` |
| a11y-mobile | Search results listbox contains group headings and wrapper divs that are not valid listbox children | `apps/website/src/frontend/components/commerce/search-bar.tsx:301` |
| cms-chatbot | The Cache-Control header on /widget.js is dead code — a root express.static mount serves the file first | `Metnmat-customer-agent-main/src/server.ts:112` |

## Refuted

These were raised and did NOT survive verification. Listed so they are not
re-raised as new discoveries later.

- /blog is fully dynamic because the page awaits searchParams at the top level — The mechanism is real but the finding as written is materially overstated and names an incomplete root cause.  WHAT I CONFIRMED BY READING CODE: 1. apps/website
- /projects goes fully dynamic to filter ~15 items it already holds in memory — The mechanism is real but the finding's load-bearing impact statement is false, and its root cause is misdiagnosed.  VERIFIED TRUE: apps/website/src/app/project
- /shop/all is dynamic AND refetches the entire 500-product catalogue at depth=1 per request — 1. CITED CODE EXISTS — partially as described.  apps/website/src/app/shop/all/page.tsx:51-56 reads exactly as quoted: `const sp = await searchParams; const [all
- Every blog article and project case-study page is fully dynamic via draftMode() — The cited lines exist verbatim (blog/[slug]/page.tsx:34 and :49; projects/[slug]/page.tsx:28 and :51), and it is true that neither route has generateStaticParam
- Every dynamic page pays ~9 layout CMS round-trips over the public HTTPS hostname — Verified by reading source. The cited lines exist and say what is claimed, and the public-hostname hairpin is genuine (no loopback override exists). But the cla
- Marquee duplicates its children, doubling ~62 partner-logo blocks into the homepage HTML — Read marquee.tsx, trusted-by.tsx, page.tsx, cms.ts, placeholder.ts, tailwind.config.ts, seed.ts and the Caddyfile.  The cited code exists and the mechanism (chi
- Homepage projects carousel puts all 15 slides in the DOM and also ships the array as client props — CODE FACTS: every cited line is accurate. I confirmed each one.  - apps/website/src/app/page.tsx:51-53 is verbatim as quoted: `{home.show.projects && orderedPro
- ~39 next/image instances emit long srcsets of absolute CMS URLs, duplicated in the flight payload — The cited lines all exist verbatim, but the mechanism that produces the claimed magnitude is wrong in three independently verifiable ways, and one of the two mu
- getFeaturedProducts silently falls back to fetching all 500 products at depth=1 — Read cms.ts:32-42, 176-208; page.tsx, hero.tsx:104, product-mosaic.tsx:80, products-preview.tsx:8, shop/page.tsx:27, search/page.tsx:37; seed.ts:429-484 and 165
- account-menu clears its own spinner one line before it starts the navigation — The cited lines exist verbatim (apps/website/src/frontend/components/commerce/account-menu.tsx:84-96: setSigningOut(true) -> await fetch("/api/account/logout") 
- Pagination is bare Links with no pending state, on routes that render dynamically — The cited code exists verbatim (pagination.tsx:42 and 52-64; the full 69-line file contains no useTransition/isPending), and there is no global fallback — no ro
- "Continue with Google" is a plain anchor full-page navigation with no pending state — The literal code observation is accurate but the mechanism and impact are materially overstated, so the finding as written does not stand.  VERIFIED AS CLAIMED:
- StoreProvider and QuoteProvider hand out a fresh context object on every render — The cited code exists exactly as quoted, but the root-cause-to-impact mechanism is wrong and the proposed fix is a no-op.  VERIFIED TRUE (read directly): - apps
- Cart and wishlist header icons opt out of prefetch, so opening the cart is an uncached round-trip — The cited code exists verbatim and the mechanism is real, but it is a deliberate, documented, and favorable trade-off rather than a defect — and the claim's pre
- Verified fine — the cart interaction path itself has no missing feedback — This "finding" asserts that nothing is broken, and reading the source confirms that assertion — so there is no defect to carry.  Verified line by line: - apps/w
- Radix dropdown never unmounts after closing: body stays scroll-locked forever and an invisible menu keeps swallowing clicks — The cited code is real but the claimed root cause is not supported by the source or the shipped CSS.  VERIFIED TRUE (parts of the claim): - apps/website/src/fro
- Consent bar permanently covers the bottom 36% of a phone screen with no reserved page padding — The cited code exists verbatim and the "no reserved padding" mechanism is real — I confirmed by reading apps/website/src/frontend/components/legal/consent-banne
- QuoteDrawer is mounted on every page even when closed, keeping a full form in the tab order — and it is why the root overflow hack exists — REFUTED — the central mechanism is explicitly handled in the source, and the claimant appears to have read a stale copy of the file.  1. The cited line numbers 
- Filter drawer uses a click-catching backdrop div and never traps or restores focus — Read the full cited file plus the reference and convention sources. The cited code exists verbatim, but the claimed mechanism and claimed impact are both wrong,
- MobileNav yanks focus back to the hamburger on route change, and focuses without preventScroll under smooth scrolling — I read apps/website/src/frontend/components/layout/mobile-nav.tsx in full, apps/website/src/frontend/components/layout/site-header.tsx, apps/website/src/app/glo
- Blog submission: which declaration checkbox blocked the submit is announced only to screen readers — The literal code fact is real but the claim is materially overstated, and its central piece of user-facing evidence is false.  VERIFIED TRUE: - apps/website/src
- Checkout renders no <form> element, so Enter never submits and the pending state is not announced — The literal code observations are accurate, but the claimed impact is materially overstated and the supporting comparison is cherry-picked.  VERIFIED TRUE: - ap
- No shared submit-state or notification primitive: six hand-rolled status shapes and the field style duplicated across 8 files — Refuted as a defect. The file inventory is accurate, but this is a code-organization observation with zero demonstrated user-facing failure, and its central evi
- Email sending is fully inline and sequential, so provider latency becomes submission latency and then a false failure — The *shape* of the code is as described, but two of the three evidence citations are wrong and the claimed failure mode has no verified trigger in this deployme
- Attachment contentType is collected and then silently dropped before the Resend call — The literal code citations are all accurate, but the claimed impact is materially overstated and both of its concrete triggers are unreachable or outright false
- Product identity in the RFQ and both emails is asserted by the client and never verified against the catalogue — The cited code is accurately quoted and the mechanism is real — but the finding is materially overstated, and its stated impact is factually false.  VERIFIED AS
- Google Cloud Console must register exactly https://www.metnmat.com/api/account/google/callback on a Web application client — Every cited line exists and says what is claimed, but the claim describes correct code and an already-documented deployment prerequisite, not a defect.  VERIFIE
- CSRF state is compared with a non-constant-time !==, unlike every other secret comparison in the codebase — The cited code is quoted accurately — apps/website/src/app/api/account/google/callback/route.ts:63 really is `if (oauthError || !code || !state || !stateCookie 
- googleConfigured() treats the published placeholder PLACEHOLDER_SET_ME as a configured client — Verified every cited line by reading source. The quotes are all accurate: google-oauth.ts:24 is indeed a bare `Boolean(clientId() && clientSecret())`, placehold
- The Google profile photo is stored on every sign-in but nothing ever reads it — I read the cited file in full and confirmed every factual assertion the claim makes. The write at callback/route.ts:83 exists, the CMS persists it (Customers.ts
- Authorization codes and CSRF state are written to the Caddy access log in the clear — REFUTED AS FILED — the mechanism is real and deployed, but the specific vector is unreachable in production, so the claimed impact ("every sign-in persists a li
- google-oauth.ts has no test coverage despite being the security boundary of the flow — Opened apps/website/src/backend/lib/google-oauth.ts in full and confirmed the cited lines exist exactly as quoted: :42 the backslash rejection, :46 the origin c
- CMS WEBSITE_URL is build-time-only and fails open to http://localhost:3000, which would make every password-reset email link dead — The claim's code citations are all accurate, and its mechanism is theoretically sound — WEBSITE_URL genuinely is not inlined at build time (no env block in apps
- The header's primary CTA "Get a Quote" is a modal-only <button> even though a real /quote page exists — CITED CODE — CONFIRMED VERBATIM. request-quote-button.tsx:60-72 is `<button type="button" data-track="get-a-quote" onClick={() => openModal()}>` with no href; R
- Upstash rate-limit fetch has no timeout and runs first on all 31 rate-limited API routes — The claim quotes rate-limit.ts:41-50 correctly and its call-site facts all check out (31 routes; limitRate first in handler at search:28, quote:73, checkout/cre
- /blog and /blog/[slug] are permanently uncacheable — the repo already documents the fix and applied it only to /shop/p — The claim pins the defect at apps/website/src/app/blog/[slug]/page.tsx:34 (the draftMode call) and asserts two independent causes. The cited line is not a defec
- getCurrentCustomer bypasses the React cache() dedupe the rest of the data layer uses — every account page fetches /me twice — The claim's *citations* are all accurate, but its *mechanism* is wrong: the duplicate fetch never reaches the network.  WHAT I CONFIRMED IS TRUE (read directly)
- Product detail page downloads the entire 500-product catalogue at depth=1 to pick a few related items — The literal code reading is accurate, but the mechanism and impact are refuted by two cache layers and one payload-size error the claimer did not check.  VERIFI
- One global "cms" cache tag — any content save cold-starts the data cache for the whole site — VERIFIED AS CLAIMED (all cited lines exist verbatim): - apps/website/src/app/api/revalidate/route.ts:15 is exactly `revalidateTag("cms");`. Minor inaccuracy: it
- No field projection on any CMS query — list endpoints return every field of every document — VERIFIED TRUE (the factual core): - `apps/website/src/frontend/lib/cms.ts:490-493` — `getProjects()` really does issue `/api/projects?depth=1&limit=200&sort=ord
- Homepage falls back to the full 500-product catalogue when no product is flagged featured — The quoted code exists verbatim (cms.ts:201-208; products-preview.tsx:8), but the claim's trigger condition, magnitude and cost model are all contradicted by so
- No timeouts anywhere in orders.service.ts — the Razorpay webhook is a long chain of unbounded CMS calls — The code observation is accurate but the defect mechanism is not. VERIFIED TRUE: apps/website/src/backend/services/orders.service.ts has 9 fetch call sites (87,
- QuoteDrawer never unmounts — every page ends with an off-screen dialog holding ~10 tabbable form fields — REFUTED — the claim's load-bearing assertion ("There is no hidden, inert, aria-hidden... grep for `inert` across apps/website/src returns no usage in any compon
- 19 form controls across the two quote forms have no accessible name — labels are detached <label> elements — Read both files in full plus ui/field.tsx, attachment-uploader.tsx, app/layout.tsx and request-quote-button.tsx.  Confirmed: the detached-label pattern is genui
- Touch targets below 44px across the header, pagination, gallery and every dialog close button — Every cited line exists and measures as claimed — I read all of them. site-header.tsx:16-17 iconLink is h-10 w-10 (40px) used at :40 and :47; cart-button.tsx:30
- Header row overflows its container at the sm breakpoint, where clip (not scroll) hides the overflow — I verified every cited line and then measured the rendered result on the live production site, which is the browser check the claim itself said it needed.  Code
- Password reveal toggle is removed from the tab order and is a 32px target — The cited code exists verbatim, but the claim is materially overstated on scope, on the standard cited, and on the mobile mechanism. Half the named surfaces — t
- Chat widget never appears for any visitor who ignores the consent bar — the gate waits for a decision the site never forces — The cited code exists verbatim and the mechanism is real, but the claimed impact is materially overstated: the gate is not what makes the chatbot unreachable, a
- A single coarse "cms" cache tag means any content save purges the entire site's data cache — The cited code exists exactly as described, but the defect framing and impact are wrong.  CONFIRMED FACTS (all read directly): - apps/website/src/app/api/revali
- Purpose-scoped internal keys are never provisioned, so every server-to-server path collapses onto one shared secret — The cited code exists verbatim, but the claim's central inference is unsound and its sharpest consequence is already closed.  CONFIRMED BY READING: - apps/dashb

