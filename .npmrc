# Flat node_modules (no symlinks) — works on Windows/OneDrive and Linux CI.
# Do not set store-dir here; use a machine-local path if needed (not committed).
node-linker=hoisted
strict-peer-dependencies=false
